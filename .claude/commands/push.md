# Push para o GitHub

1. Execute `git status` para verificar o estado atual do repositório
2. Execute `git add -A` para adicionar todas as mudanças ao staging
3. Execute `git commit -m "mensagem descritiva"` com base nas alterações identificadas
4. Execute `git push` para enviar as mudanças para o branch atual no GitHub
